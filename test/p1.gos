declare n = 7;
declare r = n/2;
declare count = 2;
declare set = 0;

declare one = 1;
declare zero = 0;

while count<=r && set == 0 do {
    if n%count == 0 then {
        assign set = 1;
    }
    assign count = count + 1;
};

print(set);